#include<iostream>
using namespace std;

int main(){
    int a=4;
    int b=6;
    cout<<"the value of a&b is "<<(a&b)<<endl;
     cout<<"the value of a|b is "<<(a|b)<<endl;
      cout<<"the value of ~a is "<<~(a)<<endl;
       cout<<"the value of a^b is "<<(a^b)<<endl;
        return 0;
}